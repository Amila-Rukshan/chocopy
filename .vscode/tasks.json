{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Build (Debug)",
        "type": "shell",
        "linux": {
          "command": "rm -rf build && mkdir -p ${workspaceFolder}/build && cd ${workspaceFolder}/build && cmake -G Ninja -DCMAKE_BUILD_TYPE=Debug .. && cmake --build ."
        },
        "group": {
          "kind": "build",
          "isDefault": true
        }
      },
    ]
}